# negociacion (dominio)

Sub-wiki auto-contenido dentro de `dominio/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: dominio`, `tema: negociacion`.

## Contexto
Negociación: método Harvard, BATNA, ZOPA, valor creado vs repartido. Cross-ref:
`dominio/juridico`, `dominio/leyes` (derecho-contratos), `dominio/ventas`,
`rol/legal-counsel`, `rol/founder-ceo`.
