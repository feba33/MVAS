# Registro de operaciones (log.md)

Registro cronológico **append-only** de la entidad `operaciones`.
Prefijo: `## [YYYY-MM-DD] <tipo> | <título>` (parseable: `grep "^## \[" log.md`).

## [2026-07-09] setup | Nodo wiki de operaciones
- Creada la superstructura recursiva de la entidad `operaciones` dentro de `dominio/`:
  `raw/`, `index.md`, `log.md` y `README.md`.
- Demuestra el principio: toda la info de operaciones vive solo en esta carpeta.

## [2026-07-09] ingest | Operaciones (cadena de suministro, lean, Six Sigma, logística, planificación de capacidad)
- Fuente: conocimiento general (reference-only).
- Página: `operaciones.md`. Cobertura: supply chain, lean, Six Sigma, logistics y capacity planning. Cross-refs a `rol/operations-lead` y `dominio/recursos-humanos`.
