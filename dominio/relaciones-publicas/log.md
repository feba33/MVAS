# Registro de relaciones-publicas (log.md)

Registro cronológico **append-only** de la entidad `relaciones-publicas`.
Prefijo: `## [YYYY-MM-DD] <tipo> | <título>` (parseable: `grep "^## \[" log.md`).

## [2026-07-11] setup | Nodo wiki de relaciones-publicas
- Creada la superstructura recursiva de la entidad `relaciones-publicas` dentro de `dominio/`:
  `raw/`, `index.md`, `log.md` y `README.md`.
- Principio: toda la info de relaciones-publicas vive solo en esta carpeta.

## [2026-07-11] ingest | Relaciones públicas (PR)
- Fuente: Wikipedia + síntesis.
- Página: `relaciones-publicas.md`.
- Reference-only (URL en frontmatter; sin raw/).
