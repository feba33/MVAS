# estadistica (dominio)

Sub-wiki auto-contenido dentro de `dominio/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: dominio`, `tema: estadistica`.

## Contexto
Disciplina de datos: estadística descriptiva, probabilidad, inferencia y ciencia de
datos. Cross-ref: `dominio/computacion` (ml-engineering, data-engineering),
`dominio/finanzas`, `dominio/marketing` (analítica), `rol/data-scientist`.
