# economia (dominio)

Sub-wiki auto-contenido dentro de `dominio/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: dominio`, `tema: economia`.

## Contexto
Disciplina macroeconómica: PIB, inflación, desempleo, política monetaria, bancos
centrales, política fiscal, ciclo económico e indicadores. Cross-ref:
`dominio/finanzas`, `dominio/estrategia`, `sustrato/*` (regímenes por país).
