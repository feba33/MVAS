# Registro de regtech (log.md)

Registro cronológico **append-only** de la entidad `regtech`.
Prefijo: `## [YYYY-MM-DD] <tipo> | <título>` (parseable: `grep "^## \[" log.md`).

## [2026-07-13] setup | Nodo wiki de regtech
- Creada la superstructura recursiva de la entidad `regtech` dentro de `dominio/`:
  `raw/`, `index.md`, `log.md` y `README.md`.
- Principio: toda la info de regtech vive solo en esta carpeta.

## [2026-07-13] ingest | RegTech (tecnología regulatoria)
- Fuentes: Wikipedia (EN) — reference-only (sin raw/).
- Página: `regtech.md`.
