# raw/ — fuentes de inteligencia-artificial

Este directorio contiene fuentes crudas (inmutables). Para fuentes web (URL) se
cita SOLO la URL en el frontmatter `fuente` de cada página; no se copia contenido aquí.
