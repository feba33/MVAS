# raw/ de uruguay (fuentes inmutables)

Las fuentes web se citan SOLO en el frontmatter `fuente` de cada página (reference-only).
No se copia contenido web a esta carpeta. Si el usuario deposita documentos locales
(PDF, texto), guárdense aquí sin modificar.
