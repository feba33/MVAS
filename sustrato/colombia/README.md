# colombia (sustrato)

Sub-wiki auto-contenido dentro de `sustrato/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: sustrato`, `tema: colombia`.
- Cobertura: marco normativo, tributario y de protección de datos que condicionan
  operar en Colombia (personas naturales y jurídicas, nacionales y extranjeras).
