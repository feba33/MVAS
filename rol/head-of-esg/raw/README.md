# raw/ — fuentes crudas (centralizadas en raíz)

Este nodo NO almacena raw/ locales: el repo MVAS mantiene UN solo raw/ en la raíz
(centralizado). Las fuentes web se citan por URL en el frontmatter (reference-only).
