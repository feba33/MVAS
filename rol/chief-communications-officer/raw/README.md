# raw/ (fuentes inmutables)

Esta carpeta aloja fuentes depositadas localmente por el usuario (inmutables).
Para fuentes web, NO se copia contenido: se cita la URL en el frontmatter `fuente:`
del archivo de página (principio reference-only del skill MVAS).
