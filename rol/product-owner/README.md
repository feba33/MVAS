# product-owner (rol)

Sub-wiki auto-contenido dentro de `rol/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: rol`, `tema: product-owner`.

## Relación
- Rol dentro de Scrum; trabaja con `rol/scrum-master` y el Development Team.
- Cross-ref: `rol/product-lead`, `dominio/producto/producto.md`, `rol/agile-coach`.
