# data-lead (entidad de rol)

Sub-wiki auto-contenido dentro de `rol/`. Arquitectura LLM Wiki recursiva: todo lo relativo a data-lead vive solo aquí.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas `*.md` con YAML frontmatter.

## Convenciones
- Frontmatter: `capa: rol`, `tema: data-lead`.
