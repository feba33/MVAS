# data-scientist (rol)

Sub-wiki auto-contenido dentro de `rol/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: rol`, `tema: data-scientist`.

## Contexto
Data Scientist: extrae valor de datos con estadística, ML y programación. Cross-ref:
`rol/data-engineer`, `dominio/estadistica`, `dominio/computacion` (ml-engineering,
data-engineering), `dominio/marketing` (analítica).
