# raw/ — fuentes de technical-writer

Fuentes crudas inmutables de este nodo. Para fuentes web se cita SOLO la URL en el
frontmatter `fuente` de cada página (reference-only); no se copia contenido aquí.
