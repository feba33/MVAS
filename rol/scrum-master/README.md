# scrum-master (rol)

Sub-wiki auto-contenido dentro de `rol/`. Arquitectura LLM Wiki recursiva.

## Superstructura
- `raw/` — fuentes (inmutables).
- `index.md` — catálogo.
- `log.md` — registro.
- páginas con YAML frontmatter.

## Convenciones
- `capa: rol`, `tema: scrum-master`.

## Relación
- Rol dentro de Scrum; sirve al PO, al Development Team y a la organización.
- Cross-ref: `rol/product-owner`, `rol/agile-coach`, `rol/engineering-manager`.
