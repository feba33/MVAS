# Registro de controller (log.md)

Registro cronológico **append-only** de la entidad `controller`.
Prefijo: `## [YYYY-MM-DD] <tipo> | <título>` (parseable: `grep "^## \[" log.md`).

## [2026-07-11] setup | Nodo wiki de controller
- Creada la superstructura recursiva de la entidad `controller` dentro de `rol/`:
  `raw/`, `index.md`, `log.md` y `README.md`.
- Principio: toda la info de controller vive solo en esta carpeta.

## [2026-07-11] ingest | Controller (controlador financiero)
- Fuentes: Coefficient (controller checklist), VelvetJobs (corporate controller job description) — reference-only.
- Página: `controller.md`. Supervisa contabilidad y reporting; cierre mensual (6-10 días mid-market),
  reconciliaciones, estados bajo GAAP; controles internos y SOX; CPA/CMA; de bookkeeping a estratégico.
  Cross-ref contabilidad, gobierno-corporativo, cfo, auditor-interno, finanzas.
- Reference-only (URL en frontmatter; sin raw/).
