# raw/ — fuentes de bookkeeper

Este directorio almacena fuentes depositadas localmente (inmutables).
Para fuentes web, se cita la URL en el frontmatter de la página (reference-only); no se copia contenido aquí.
