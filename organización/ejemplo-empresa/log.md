# Registro de ejemplo-empresa (log.md)

Registro cronológico **append-only** de la entidad `ejemplo-empresa`.
Prefijo: `## [YYYY-MM-DD] <tipo> | <título>` (parseable: `grep "^## \[" log.md`).

## [2026-07-09] setup | Nodo wiki de ejemplo-empresa
- Creada la superstructura recursiva de la entidad `ejemplo-empresa` dentro de `organización/`:
  `raw/`, `index.md`, `log.md` y `README.md`.
- Demuestra el principio: toda la info de ejemplo-empresa vive solo en esta carpeta.

## [2026-07-09] ingest | Empresa ficticia NovaTech
- Fuente: conocimiento general — reference-only (raw = cita; no se crea archivo en `raw/`).
- Página: `ejemplo-empresa.md`. Empresa ficticia «NovaTech» (startup SaaS, 50 empleados,
  México/EE.UU.): misión, estructura (org chart), producto NovaFlow, mercado, métricas clave
  y decisiones de ejemplo.
